# Copia este archivo a .env.local y llena tus valores reales.
# .env.local NUNCA debe subirse a git ni compartirse (está en .gitignore).

# true = usa datos de ejemplo, no llama a los GLPI reales (para ver la app funcionando ya)
# false = se conecta de verdad a los 5 sistemas
DEMO_MODE=true

# Credenciales compartidas (se usan si no defines una específica por sistema abajo)
GLPI_USER=martin.vega
GLPI_PASSWORD=CAMBIA_ESTA_CONTRASENA

# --- CNS ---
GLPI_CNS_URL=https://opcenter.cns-ipicyt.mx/cns
GLPI_CNS_APP_TOKEN=
GLPI_CNS_USER_TOKEN=
# Pon esto en "true" SOLO si el error es "fetch failed" y confirmas que es
# por certificado SSL autofirmado/incompleto (reduce la seguridad de esa conexión):
GLPI_CNS_INSECURE_TLS=false

# --- UnADM ---
GLPI_UNADM_URL=https://opcenter-unadm.cns-ipicyt.mx
GLPI_UNADM_APP_TOKEN=
GLPI_UNADM_USER_TOKEN=

# --- Prepa ---
GLPI_PREPA_URL=https://opcenter-prepa.cns-ipicyt.mx
GLPI_PREPA_APP_TOKEN=
GLPI_PREPA_USER_TOKEN=

# --- SECIHTI ---
GLPI_SECIHTI_URL=https://opcenter-secihti.cns-ipicyt.mx
GLPI_SECIHTI_APP_TOKEN=
GLPI_SECIHTI_USER_TOKEN=

# --- Mujeres ---
GLPI_MUJERES_URL=https://opcenter-mujeres.cns-ipicyt.mx
GLPI_MUJERES_APP_TOKEN=
GLPI_MUJERES_USER_TOKEN=

# Opcional: webhook (Slack/Discord/Teams) para alertas de SLA vencido
ALERT_WEBHOOK_URL=

# Opcional: activa sugerencias con Claude en el asistente (si no se define,
# usa un motor de reglas local que funciona sin costo ni configuración)
ANTHROPIC_API_KEY=

# Usado por scripts/dailyReport.js para llamar a la app si corre aparte
APP_URL=http://localhost:3000
